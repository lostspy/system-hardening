---
 - name: playbook for RedHat Hardening
   hosts: aws_ec2
   gather_facts: yes
   tasks:
     - name: "Configure System Ctrl"
       sysctl: 
         name: "{{ item.name }}"
         state: latest
         value: "{{ item.value }}"
       with_items: "{{ harden }}"
